<Window x:Class="Hexxagon.GridWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Hexxagon"
    xmlns:controls="clr-namespace:Hexxagon.Controls"
        WindowStartupLocation="CenterScreen"
    Title="GridTest" SizeToContent="WidthAndHeight">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Bestand">
                <MenuItem Header="_Random Spel Starten" Command="{Binding BrowseCommand}" />
                <MenuItem Header="_Test" Command="{Binding TestCommand}" />
                <MenuItem Header="_Spel Opslaan" Command="{Binding SaveCommand}" />
                <MenuItem Header="_Spel laden" Command="{Binding LoadCommand}" />
                <MenuItem Header="_Afsluiten" Command="{Binding CloseCommand}" />
            </MenuItem>
        </Menu>
        <Border BorderThickness="20" BorderBrush="Black">
        <ContentControl Content="{Binding ViewModel}"/>
        </Border>
    </DockPanel>    
</Window>
